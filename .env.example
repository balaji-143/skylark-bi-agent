# monday.com personal API token (Admin -> Connections -> API in your monday.com account).
# NEVER commit the real value or paste it into chat/screenshots.
MONDAY_API_TOKEN=

# Free-tier key from https://aistudio.google.com/app/apikey
GEMINI_API_KEY=

# Filled in automatically by import_csv_to_monday.py after it creates the boards
# (or set manually if you already know the board IDs).
DEALS_BOARD_ID=
WORK_ORDERS_BOARD_ID=
